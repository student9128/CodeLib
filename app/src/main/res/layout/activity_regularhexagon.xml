<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="80dp">

    <com.kevin.codelib.customview.RegularHexagon
        android:id="@+id/viewRegularHexagon"
        android:layout_width="300dp"
        android:layout_height="300dp"
        app:contentColor="#FFBEBEBE"
        app:hexagonWidth="12dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHorizontal_bias="0.495"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:progress="50"
        app:text="男"
        app:textSize="80dp" />

    <SeekBar
        android:id="@+id/seekBar"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:progress="50"
        android:progressDrawable="@drawable/shape_progress"
        android:thumb="@drawable/ic_arrow_r"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>